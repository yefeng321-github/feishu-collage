{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [
    { "src": "/editor.html", "dest": "/editor.html" },
    { "src": "/", "dest": "/index.html" }
  ]
}
